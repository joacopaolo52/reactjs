import Titles from './Titles';
import Card from './Card'

function ItemListContainer() {
    return(
        <div>
            <Titles title = 'Catalogo'/>
            <Card product=' ' price='$' img = {a} />
            <Card product=' ' price='$' img = {a} />
            <Card product=' ' price='$' img = {a} />
            <Card product=' ' price='$' img = {a} />
            <Card product=' ' price='$' img = {a} />
        </div>
    )}

export default ItemListContainer;